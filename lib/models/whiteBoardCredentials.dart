class whiteBoardCredentials {
  String sdkToken;
  String roomUUID;
  String roomToken;

  whiteBoardCredentials({
    required this.sdkToken,
    required this.roomUUID,
    required this.roomToken,
  });
}
